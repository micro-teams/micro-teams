# MultiPath is published to GitHub Packages, which requires an authenticated request even for a
# public package. CI passes the workflow's own GITHUB_TOKEN; locally, export NODE_AUTH_TOKEN with a
# token that has read:packages (a classic PAT is enough).
@micro-teams:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
