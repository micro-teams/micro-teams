{
  "name": "microteams-app-checks",
  "private": true,
  "version": "0.1.18",
  "description": "The browser checks for the built web app, and the launcher that starts it. Flutter builds the app itself; this is the launcher that loads it (tool/launcher-html.mjs — ours since MultiPath 0.2.0 dropped its launcher), and the Playwright checks — service workers, offline start and the escape hatch need a real browser.",
  "type": "module",
  "scripts": {
    "launcher": "node tool/launcher.mjs build/web",
    "check:web": "bash tool/check-web.sh"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "playwright": "1.63.0"
  },
  "dependencies": {
    "@micro-teams/multipath": "^0.2.0-rc.1"
  }
}
