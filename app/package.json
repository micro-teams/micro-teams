{
  "name": "microteams-app-checks",
  "private": true,
  "version": "0.1.16",
  "description": "The browser checks for the built web app. Not part of the app — Flutter builds that. This exists because asserting service workers, offline start and the escape hatch needs a real browser, and Playwright is how that is driven.",
  "type": "module",
  "scripts": {
    "check:web": "bash tool/check-web.sh"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
