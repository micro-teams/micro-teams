{
  "name": "microteams-app-checks",
  "private": true,
  "version": "0.1.16",
  "description": "The browser checks for the built web app, and the launcher that starts it. Flutter builds the app itself; this is the multipath launcher that loads it, and the Playwright checks — service workers, offline start and the escape hatch need a real browser.",
  "type": "module",
  "scripts": {
    "launcher": "node tool/launcher.mjs build/web",
    "check:web": "bash tool/check-web.sh"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  },
  "dependencies": {
    "@micro-teams/multipath": "main"
  }
}
